# Secret + publishable (same Dashboard page). Publishable is safe to expose to the browser.
STRIPE_SECRET_KEY=sk_test_your_key_here
STRIPE_PUBLISHABLE_KEY=pk_test_your_key_here

# Must match the URL where the SPA runs (Vite default)
CLIENT_ORIGIN=http://localhost:5173
